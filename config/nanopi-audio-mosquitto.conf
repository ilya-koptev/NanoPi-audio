# nanopi-audio: MQTT broker listener on the LAN (anonymous, local network only).
# Add authentication here if the broker is reachable beyond a trusted network.
listener 1883 0.0.0.0
allow_anonymous true
